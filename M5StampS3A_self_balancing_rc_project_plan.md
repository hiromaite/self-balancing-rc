# M5StampS3A 2輪セルフバランシングRCロボット企画書

## 0. このドキュメントの目的

このドキュメントは、M5StampS3Aを中核にした2輪セルフバランシングRCロボットの企画・設計・実装・検証・改善を、Codexアプリに引き継ぐためのプロジェクト企画書である。

このプロジェクトは単なる倒立振子ロボットの製作ではなく、以下を同時に満たすことを目的とする。

- M5StampS3Aを使ったリアルタイム姿勢制御の学習
- IMU、エンコーダ、DCモーター、モータードライバ、LiPo電源系の統合学習
- 親子で遊べる2輪セルフバランシングRCの製作
- 6歳の子どもと楽しめる外装・遊び・コース設計
- 将来的な複数台レースへの拡張
- 将来的なカメラ搭載・遠隔操縦への拡張
- PS5コントローラーなどを用いた楽しい操作系への拡張
- 3Dプリンターを活用した車体・外装・車輪・交換式ボディの設計
- 制御、通信、電源、機構、UI、ログ分析を統合した実践的な開発経験の獲得

このMarkdownを新しいプロジェクトディレクトリのルートに配置し、Codexアプリにはここから以下を行わせる想定である。

- `docs/` 配下の要求仕様・アーキテクチャ・配線・安全試験計画の整備
- `firmware/` 配下のPlatformIOプロジェクト作成
- `pc_tools/` 配下のシリアルモニタ、PIDチューナー、PS5ブリッジの実装
- `cad/` 配下の機構設計メモ、寸法制約、初号機シャーシ設計支援
- 段階的な実装計画、検証計画、改善ループの管理

---

## 1. プロジェクトの背景

ユーザーは、M5StampS3Aを用いた電子工作・ファームウェア開発・センサー計測・PCアプリ連携に関心があり、すでに以下のような開発経験や関心を持っている。

- M5StampS3A / ESP32-S3 を使った組み込み開発
- PlatformIOを用いたファームウェア管理
- BLEやシリアル通信を用いたPCアプリ連携
- PythonアプリやReact/Vite/TypeScript系UIによる可視化
- センサー値の高速サンプリング、ログ取得、グラフ化
- 3Dプリンターを使った筐体・部品設計
- 子どもと一緒に楽しめるロボット工作
- 制御・通信・電源・機構を横断した学習プロジェクト

本プロジェクトでは、M5StampS3Aをマイコンとして固定しつつ、IMU、モーター、モータードライバ、バッテリー、3Dプリント機構を組み合わせ、最終的に「親子で遊べるセルフバランシングラジコン」として成立させる。

---

## 2. プロジェクトの最終像

最終的なイメージは以下の通り。

```text
操作側:
  Phase 1: PCからUSBシリアルで操作・ログ取得
  Phase 2: PC/スマホからWi-Fi UDPで操作
  Phase 3: PS5 DualSense → PC/Raspberry Pi中継 → Wi-Fi UDP → 車体
  Phase 4: FPVカメラまたはRaspberry Pi Zero 2 Wによる映像付き遠隔操作
  Phase 5: 複数台で兄弟・親子レース

車体側:
  M5StampS3A
    ├─ IMU: BNO086 / ICM-42688-P
    ├─ エンコーダ付きDCギヤードモーター ×2
    ├─ 左右独立モータードライバ
    ├─ 2S LiPo電源
    ├─ 5V DCDC制御電源
    ├─ バッテリー電圧監視
    ├─ 安全停止ロジック
    ├─ テレメトリ出力
    └─ 将来のLED/OLED/ブザー/カメラ/ToF拡張
```

---

## 3. 重要な設計方針

### 3.1 M5StampS3Aを車体制御専任にする

M5StampS3Aは車体側のリアルタイム制御を担当する。

担当する処理:

- IMU読み取り
- 姿勢推定
- 倒立制御
- エンコーダ読み取り
- モーターPWM出力
- 安全停止
- バッテリー監視
- テレメトリ送信
- 上位指令受信

担当させない方がよい処理:

- PS5コントローラーのBluetooth直接接続
- 映像配信
- 高度な画像処理
- 重いUI処理
- 複数台統合管理

理由:

- M5StampS3AはESP32-S3系でBluetooth ClassicではなくBLE中心の設計である。
- PS5 DualSenseの直接接続はESP32-S3単体では難易度・不確実性が高い。
- 姿勢制御はリアルタイム性が重要であり、映像や複雑な通信を同一MCUに背負わせない方が安全。
- カメラやPS5コントローラーはPC/Raspberry Pi/FPV系に分離する方が拡張しやすい。

---

### 3.2 PS5コントローラーは中継方式にする

PS5 DualSenseを使う場合、車体に直接接続しない。

推奨構成:

```text
PS5 DualSense
  ↓ Bluetooth / USB
PC or Raspberry Pi
  ↓ Wi-Fi UDP
M5StampS3A
```

メリット:

- M5StampS3A側は単純な速度指令だけを受け取ればよい。
- DualSenseの入力処理をPython等で扱える。
- PS5コントローラー以外の入力にも拡張しやすい。
- 通信デバッグが容易。
- 将来的に複数台レースの司令塔としてPC/Raspberry Piを使える。

---

### 3.3 初号機から余力を持たせる

最初はTTモーター級の軽量構成も検討したが、最終的に以下の理由から、やや本格的な構成を採用する。

- 将来的にカメラを搭載したい。
- 複数台でレースさせたい。
- 3Dプリント外装を載せたい。
- 子どもと遊ぶため、多少の転倒や衝撃に耐える剛性が欲しい。
- エンコーダによる速度制御・旋回制御まで学びたい。
- ただし37D級や過大なモーターは大きすぎる可能性が高い。

最終的な落としどころは、25D級エンコーダ付きメタルギヤードDCモーターである。

---

### 3.4 37D級や大型モーターは過剰寄り

オンライン上の類似事例や倒立ロボット事例を調査した結果、以下が分かった。

- 小型倒立ロボットはN20やMicro Metal Gearmotor級でも成立する。
- Pololu Balboaのような倒立ロボットは、80mm級ホイールと小型メタルギヤモーターで成立している。
- 600〜1200g級、車高120〜250mm、80mmホイール程度なら、25D級が適切な落としどころ。
- 37D級は大型・屋外・高負荷・重いカメラ用途にはよいが、家庭内/親子/小型レース用途では大きすぎる。
- 強すぎるモーターは安全性、重量、電源設計、車体剛性、転倒時の衝撃の面で不利。

したがって、最終推奨は25D級とする。

---

## 4. 最終推奨システム構成

### 4.1 車体側ハードウェア構成

```text
M5StampS3A
  ├─ I2C/SPI → BNO086
  ├─ SPI/I2C → ICM-42688-P
  ├─ PWM/DIR → 左モータードライバ
  ├─ PWM/DIR → 右モータードライバ
  ├─ GPIO割り込み → 左エンコーダA/B
  ├─ GPIO割り込み → 右エンコーダA/B
  ├─ ADC → バッテリー電圧監視
  ├─ GPIO → LED / ブザー / 緊急停止入力
  └─ USB Serial / Wi-Fi UDP → 上位PC・操作系
```

---

### 4.2 電源構成

```text
2S LiPo Battery
  ↓ XT30
  ↓ Main Power Switch
  ↓ Fuse / Polyfuse
  ↓ Power Distribution
      ├─ Motor Power → Left Motor Driver
      ├─ Motor Power → Right Motor Driver
      └─ 5V Buck DCDC
            ├─ M5StampS3A
            ├─ BNO086
            ├─ ICM-42688-P
            ├─ Motor Encoders
            ├─ LED / OLED / Buzzer
            └─ Future low-power accessories
```

設計上の重要点:

- モーター電源と制御系5V電源は分岐する。
- GNDは共通化する。
- モーター電源線とIMU/I2C/SPI信号線は可能な限り離す。
- 5V DCDCは3A以上、できれば5A級。
- モーター電源側には470〜1000µF程度の電解コンデンサを配置。
- LiPoの過放電を防ぐため、M5StampS3AのADC監視と外付けLiPoアラームを併用する。
- 充電は車体内蔵ではなく、外部RC用バランス充電器で行う。

---

## 5. 採用候補部品

### 5.1 マイコン

#### 採用: M5StampS3A

既に手元にあるため採用確定。

役割:

- リアルタイム制御
- IMU読み取り
- エンコーダ入力
- モーター制御
- 安全停止
- テレメトリ
- 上位指令受信

注意点:

- PS5コントローラーは直接接続しない。
- Bluetooth Classic依存の構成にはしない。
- USBシリアルとWi-Fi UDPを主な通信手段とする。

---

### 5.2 IMU

#### 手元部品: Bosch BNO055

使えるが、最終構成の主役にはしない。

評価:

- センサーフュージョン内蔵で姿勢角が取得しやすい。
- 初期学習や比較用には有用。
- ただし新規設計向けには古く、出力周期やレイテンシ面で制御用として最適とは言いにくい。
- 手元にあるため、BNO086やICM-42688-Pとの比較用ログ取得には使う価値が高い。

用途:

- 初期動作確認
- ログ比較
- センサーフュージョン済み姿勢角の参考値
- BNO086/ICM-42688-Pが届くまでの実験

---

#### 第一候補: BNO086

採用理由:

- BNO055より新しいセンサーフュージョン系IMU。
- 姿勢角を扱いやすい。
- 初期倒立成功までの立ち上げが楽。
- ロボット・VR/AR用途に近い。
- M5StampS3A側の姿勢推定実装負荷を下げられる。

推奨用途:

- 初期倒立制御
- センサーフュージョン済み姿勢角の取得
- ログ比較の基準
- 実装初期の成功率向上

購入候補:

- Qwiic BNO086搭載 VR IMUモジュール
- 入手先候補: スイッチサイエンス

---

#### 第二候補・併用推奨: ICM-42688-P

採用理由:

- 高速な6軸IMU。
- 生の加速度・ジャイロを使って独自フィルタを構築できる。
- 制御用としてレイテンシやサンプリング周期を管理しやすい。
- 学習効果が高い。
- 将来的にBNO086よりも制御に最適化しやすい可能性がある。

推奨用途:

- 高速姿勢推定
- 相補フィルタ / Mahony / Madgwick / EKF の実装学習
- BNO086との比較
- 最終制御用IMU候補

購入候補:

- ICM-42688-P 6軸IMUモジュール
- 入手先候補: ストロベリーリナックス、DigiKey、その他国内EC

---

### 5.3 モーター

#### 最終推奨: Pololu 25D 34:1 HP 12V Encoder付き

採用理由:

- 25D級で大きすぎない。
- 12V、約300rpm級。
- 80mmホイール使用時に速度余力がある。
- エンコーダ分解能が高い。
- 2S LiPoで初期は安全寄り、将来3Sで高出力寄りに拡張可能。
- 倒立制御、速度制御、旋回制御、レース用途のバランスが良い。

推奨数量:

- 最低2個
- 予備・比較・将来複数台を見据えるなら4個以上

想定:

- 初期運用は2S LiPo。
- PWM上限をソフトウェアで制限。
- 子どもモードでは速度・角度・旋回を強く制限。
- 将来レースモードで上限を引き上げる。

---

#### 代替: Pololu 25D 47:1 HP 12V Encoder付き

採用理由:

- 34:1よりトルク寄り。
- 約220rpm級で安全寄り。
- 子どもと遊ぶ用途には扱いやすい。
- カメラや外装で重量が増えた場合にも余裕がある。

選定基準:

- 俊敏さ・レース性を重視するなら34:1。
- 安全性・扱いやすさ・重い外装を重視するなら47:1。

---

#### 採用しない/優先度低: TTモーター級

評価:

- 安価で軽く、倒立実験には使える。
- ただし将来のカメラ搭載、複数台レース、耐久性、3Dプリント外装には余力不足。
- 教材用・比較用・予備実験用としては有用。

---

#### 採用しない/優先度低: 37D級モーター

評価:

- 強力だが大きく重い。
- 家庭内・親子・小型レース用途には過剰。
- 車体重量、電源、配線、ヒューズ、スイッチ、転倒時衝撃、3Dプリント剛性要求が増す。
- 屋外走行や重いカメラ搭載機なら将来候補。

---

### 5.4 モータードライバ

#### 推奨: Cytron MD10C級を左右1枚ずつ

採用理由:

- 25D HPモーターのピーク電流に余裕を持てる。
- 1モーター1ドライバ構成で熱と電流に余裕。
- 3.3Vロジック対応品が使いやすい。
- 20kHz級PWM対応品なら可聴ノイズも抑えやすい。
- 将来的にやや強いモーターに変えても対応しやすい。

構成:

```text
M5StampS3A
  ├─ PWM_L + DIR_L → MD10C_L → Left Motor
  └─ PWM_R + DIR_R → MD10C_R → Right Motor
```

注意点:

- TB6612FNGやDRV8833は小型モーター向けの予備・実験用。
- 25D HPを使うなら、連続1A程度のドライバは避ける。
- MD10C級はドライバとしては余裕があるが、モーターが25D級なら安全マージンとして合理的。

---

### 5.5 バッテリー

#### 推奨: 2S LiPo 1500〜2200mAh 30C以上 XT30

採用理由:

- 2S公称7.4V、満充電8.4V。
- 12Vモーターを安全寄りに低めの電圧で動かせる。
- 初期倒立・子どもモードでは十分。
- 将来速度が欲しければ3S拡張も検討できる。
- 1500〜2200mAhなら重量と運用時間のバランスがよい。
- 30C以上でピーク電流に余裕。
- XT30はこのサイズのロボットに適切。

推奨数量:

- 最低2本
- 開発効率を考えると3〜4本
- 複数台展開時は台数×2本以上

---

#### バッテリー充電

推奨:

- 車体内蔵充電は行わない。
- 外部RC用バランス充電器を使う。
- 充電・保管はLiPoセーフバッグを使う。
- 使用後は必要に応じてストレージ電圧で保管する。

購入候補:

- SkyRC B6AC Neo
- ISDT 608AC
- ISDT P10などの複数本運用向け充電器

---

### 5.6 電源保護・配線部品

必須/強く推奨:

- XT30コネクタ一式
- XT30付き電源リード
- 10Aまたは15A程度のインラインヒューズ
- DC大電流対応メインスイッチ
- 5V 3〜5A Buck DCDC
- 2S LiPo電圧アラーム
- 470〜1000µF 電解コンデンサ
- 0.1µF セラミックコンデンサ
- バッテリー電圧測定用分圧抵抗
- 16〜20AWG程度のシリコン線
- JST-XH / PH / VH / XH2.54系コネクタ
- M3ネジ、インサートナット、スペーサー
- LiPoセーフバッグ

---

## 6. 電源・安全設計

### 6.1 基本電源構成

```text
2S LiPo
  ↓ XT30
  ↓ Main Switch
  ↓ Fuse
  ↓ Power Distribution
      ├─ VMOTOR → MD10C_L
      ├─ VMOTOR → MD10C_R
      └─ Buck 5V
            ├─ M5StampS3A
            ├─ IMUs
            ├─ Encoders
            ├─ Status LEDs
            ├─ OLED/Buzzer
            └─ Future low-power peripherals
```

---

### 6.2 LiPo電圧の運用目安

2S LiPoの電圧目安:

| 状態 | 2S全体 | 1セルあたり |
|---|---:|---:|
| 満充電 | 8.4V | 4.2V |
| 公称 | 7.4V | 3.7V |
| 終了が近い | 7.2V前後 | 3.6V |
| 停止推奨 | 6.8〜7.0V | 3.4〜3.5V |
| 危険側 | 6.4V以下 | 3.2V以下 |

M5StampS3A側の安全制御案:

```text
bat_voltage < 7.2V:
  LED黄色
  速度制限

bat_voltage < 7.0V:
  LED赤
  走行停止

bat_voltage < 6.8V:
  モーター完全停止
  復帰には再起動または明示操作が必要
```

---

### 6.3 安全停止条件

最低限入れるべき安全停止条件:

- 傾き角が±35°を超えた
- IMU更新が途絶えた
- エンコーダ値が異常
- 通信途絶が300ms以上続いた
- バッテリー電圧が停止閾値を下回った
- 起動直後で安全確認が終わっていない
- 手動停止コマンドを受け取った
- 緊急停止入力が入った
- モーター出力が一定時間飽和した
- 倒れた状態でモーターが回り続けようとしている

---

### 6.4 子どもと遊ぶための安全モード

必須モード:

- こどもモード
- 開発者モード
- レースモード
- キャリブレーションモード

こどもモードの制限:

- 最大PWM制限
- 最大目標角度制限
- 最大旋回速度制限
- 通信途絶時の即停止
- 傾き過大時の即停止
- R2などのデッドマン入力を押している間だけ動作
- 起動時は必ずモーター無効
- 転倒後は手動復帰が必要

---

## 7. 制御設計

### 7.1 基本制御

初期制御はPD制御から始める。

```text
balance_power = Kp * angle_error + Kd * gyro_rate
```

Kiは最初は使わない。  
倒立が安定してから、必要に応じてごく小さく導入する。

---

### 7.2 左右モーター出力

```text
left_motor  = balance_power + turn_power
right_motor = balance_power - turn_power
```

前進/後退は直接PWM指令ではなく、目標角度を変える。

```text
target_angle = forward_command * max_target_angle
```

初期値:

```text
max_target_angle = 3〜5 degrees
```

---

### 7.3 エンコーダを使った速度制御

倒立が成立した後に、エンコーダ速度を用いて速度制御を追加する。

```text
speed_error = target_speed - measured_speed
target_angle = K_speed * speed_error
```

これにより、前進・後退指令が目標角度に変換され、倒立しながら速度制御する構成になる。

---

### 7.4 制御周期の目安

| 処理 | 初期目標 |
|---|---:|
| IMU読み取り | 200〜1000Hz |
| 姿勢推定 | 200〜1000Hz |
| 倒立制御 | 200〜500Hz |
| エンコーダ更新 | 割り込み / 高頻度 |
| 通信処理 | 20〜100Hz |
| テレメトリ送信 | 10〜50Hz |
| バッテリー監視 | 1〜10Hz |

BNO086中心の初期実装ではセンサー出力周期に合わせる。  
ICM-42688-P主体に移る場合は、より高周期化を狙う。

---

### 7.5 姿勢推定の段階

段階的に以下を実装する。

1. BNO086の姿勢角を使う
2. BNO086の姿勢角 + ジャイロレートを使う
3. ICM-42688-Pの生データで相補フィルタを実装する
4. Mahony/Madgwickフィルタを試す
5. 必要ならEKFを検討する
6. BNO055/BNO086/ICM-42688-Pのログを比較する

---

## 8. 通信設計

### 8.1 初期通信: USB Serial

最初はUSBシリアルで十分。

用途:

- テレメトリ出力
- PIDゲイン変更
- モーター有効/無効
- 緊急停止
- キャリブレーション
- ログ取得

ログ形式はJSON Linesを推奨。

例:

```json
{"t":12345,"angle":1.24,"gyro":-0.02,"enc_l":120,"enc_r":118,"pwm_l":32,"pwm_r":31,"bat":7.83,"mode":"idle"}
```

---

### 8.2 次段階通信: Wi-Fi UDP

上位PC/スマホ/Raspberry Piから速度指令を送る。

指令例:

```json
{"vx":0.2,"wz":0.0,"enable":true,"mode":"kid"}
```

フィールド案:

- `vx`: 前後指令
- `wz`: 旋回指令
- `enable`: デッドマン相当
- `mode`: kid/dev/race
- `estop`: 緊急停止
- `seq`: シーケンス番号
- `ts`: タイムスタンプ

通信途絶時:

```text
last_command_age > 300ms:
  target_speed = 0
  target_turn = 0
  motor_enable = false
```

---

### 8.3 PS5コントローラー中継

構成:

```text
PS5 DualSense
  ↓ Bluetooth / USB
PC or Raspberry Pi
  ↓ UDP
M5StampS3A
```

推奨マッピング:

| 入力 | 機能 |
|---|---|
| 左スティック上下 | 前進/後退 |
| 左スティック左右 | 旋回 |
| R2 | デッドマン/有効化 |
| ○ | 緊急停止 |
| △ | モード切替 |
| L1/R1 | 速度レベル変更 |
| Option | キャリブレーション/リセット |

---

## 9. カメラ・遠隔操縦の将来構成

### 9.1 FPVカメラ方式

レース用途には低遅延のFPVカメラ方式が有力。

```text
FPV Camera + 5.8GHz VTX
  ↓
FPV Monitor / Goggles

PS5 Controller
  ↓ PC/Raspberry Pi
M5StampS3A
```

メリット:

- 低遅延
- M5StampS3Aに負荷をかけない
- レース向き
- 制御系と映像系を分離できる

---

### 9.2 Raspberry Pi Zero 2 W方式

将来的な画像処理や録画を考えるなら有力。

```text
Raspberry Pi Zero 2 W
  ├─ Camera Stream
  ├─ PS5 Controller Input
  └─ UDP Commands to M5StampS3A
```

メリット:

- 映像配信
- 録画
- Web UI
- 将来的な画像認識
- 複数台管理

デメリット:

- 電源設計が難しくなる
- 起動時間が長い
- 遅延対策が必要
- 車体重量が増える

初号機では、FPV/カメラ用マウント穴と5V電源余力だけ確保しておく。

---

## 10. 機構・3Dプリント設計

### 10.1 初号機シャーシの目的

最初からかっこいい外装を作るのではなく、調整可能な実験シャーシを作る。

目的:

- 倒立制御の成立
- 重心位置調整
- IMU取り付け位置検証
- モーター・車輪の固定剛性確認
- 配線取り回し確認
- 転倒時保護
- 将来外装の基礎

---

### 10.2 推奨寸法感

| 項目 | 推奨 |
|---|---:|
| 車輪径 | 80mm前後 |
| 車幅 | 150〜180mm |
| 高さ | 180〜250mm |
| 重量 | 700〜1200g程度まで |
| バッテリー位置 | 上下調整可能 |
| IMU位置 | 車軸中心の真上付近 |
| モーター固定 | 3Dプリント + 必要なら金属ブラケット |
| 外装 | 後付け交換式 |
| カメラ | 将来マウント穴のみ確保 |

---

### 10.3 機構設計の重要点

- IMU固定部は絶対にグラつかせない。
- IMUはできるだけ車体の剛性が高い中央部へ置く。
- バッテリー位置を上下に調整できるようにする。
- M5StampS3AのUSBにアクセスできる。
- モータードライバは放熱と配線の余裕を持たせる。
- 電源スイッチは外から押しやすい位置に置く。
- 転倒時にM5StampS3AやIMUが床に直接当たらないようにする。
- 車輪周りに指巻き込み防止ガードを検討する。
- 子どもが触る部分は角を丸める。
- 外装は軽く、後付け式にする。
- 上部にカメラマウント用のM2/M3穴を用意する。
- 複数台化を見据えて、シャーシ部品は共通化しやすくする。

---

## 11. 開発ディレクトリ構成案

```text
self-balancing-rc/
  README.md
  PROJECT_PLAN.md  ← このドキュメントを配置してもよい

  docs/
    requirements.md
    architecture.md
    bom.md
    wiring.md
    gpio_assignment.md
    power_design.md
    safety_test_plan.md
    control_design.md
    communication_protocol.md
    tuning_log.md
    build_log.md

  firmware/
    platformio.ini
    src/
      main.cpp
      config.h
      imu_bno086.cpp
      imu_bno086.h
      imu_icm42688.cpp
      imu_icm42688.h
      motor_driver.cpp
      motor_driver.h
      encoder.cpp
      encoder.h
      battery_monitor.cpp
      battery_monitor.h
      balance_controller.cpp
      balance_controller.h
      safety.cpp
      safety.h
      telemetry.cpp
      telemetry.h
      command_parser.cpp
      command_parser.h
    include/
    lib/
    test/

  pc_tools/
    serial_monitor.py
    pid_tuner.py
    ps5_bridge.py
    udp_console.py
    log_plotter.py
    requirements.txt

  cad/
    chassis_v1/
      README.md
      dimensions.md
      export/
    wheel_v1/
    body_v1/
    camera_mount_v1/

  data/
    logs/
    tuning/
    sensor_comparison/

  scripts/
    format_logs.py
    analyze_step_response.py
```

---

## 12. まずCodexに作らせるべきドキュメント

以下を `docs/` に作成する。

### 12.1 `docs/requirements.md`

記載内容:

- プロジェクト目的
- 初号機の成功条件
- 将来拡張条件
- 安全要件
- 子どもと遊ぶための制約
- 操作モード
- 非目標

初号機の成功条件案:

```text
- 手を離して10秒以上自立する
- 30秒以上安定自立を目指す
- 低速で前進/後退できる
- その場旋回できる
- 転倒時にモーター停止する
- 通信途絶時に停止する
- 電池電圧低下時に停止する
- USBシリアルでログが取れる
- PIDゲインをPCから変更できる
```

---

### 12.2 `docs/architecture.md`

記載内容:

- システム全体図
- 車体側責務
- PC/Raspberry Pi側責務
- 将来のカメラ構成
- PS5中継構成
- 複数台レース構成

---

### 12.3 `docs/bom.md`

記載内容:

- 採用部品
- 候補部品
- 購入先
- 数量
- 用途
- 選定理由
- 未確定事項

主要部品:

- M5StampS3A
- BNO086
- ICM-42688-P
- Pololu 25D 34:1 HP 12V Encoder付き
- 代替: Pololu 25D 47:1 HP 12V Encoder付き
- Cytron MD10C級ドライバ ×2
- 2S LiPo 1500〜2200mAh XT30
- SkyRC B6AC NeoまたはISDT 608AC
- 5V 3〜5A DCDC
- XT30
- ヒューズ
- LiPoセーフバッグ
- 80mmホイール
- M3ネジ、インサートナット、スペーサー
- LED/OLED/ブザー
- 電圧アラーム

---

### 12.4 `docs/wiring.md`

記載内容:

- 電源配線図
- 信号配線図
- GND方針
- モーター線とセンサー線の分離
- I2C/SPI配線
- エンコーダ配線
- モータードライバ配線
- ADC分圧回路

---

### 12.5 `docs/gpio_assignment.md`

M5StampS3AのGPIO割当案を作る。  
実機の使用可能GPIO、起動時状態、I2C/SPI/PWM/ADC制約を確認した上で確定する。

必要な信号:

```text
I2C SDA
I2C SCL
SPI SCK
SPI MOSI
SPI MISO
SPI CS_IMU
PWM_LEFT
DIR_LEFT
PWM_RIGHT
DIR_RIGHT
ENC_LEFT_A
ENC_LEFT_B
ENC_RIGHT_A
ENC_RIGHT_B
BATTERY_ADC
EMERGENCY_STOP_IN
LED_STATUS
BUZZER
```

注意:

- M5StampS3Aの起動時に意図せずHIGHになるGPIOをモーター有効信号に使わない。
- モータードライバのENABLE相当は安全側に倒す。
- 起動直後は必ずモーター無効。
- ADC入力は2S LiPoの最大8.4Vを安全に分圧する。

---

### 12.6 `docs/power_design.md`

記載内容:

- 2S LiPoの採用理由
- 3S拡張時の注意
- 5V DCDC要件
- ヒューズ選定
- 電源スイッチ選定
- 過放電保護
- 電源ノイズ対策
- バッテリー運用ルール

---

### 12.7 `docs/safety_test_plan.md`

記載内容:

- ベンチテスト手順
- 電源投入前チェック
- モーター単体テスト
- IMU単体テスト
- エンコーダ単体テスト
- 車体を浮かせた状態でのテスト
- 手支え倒立テスト
- 転倒時停止テスト
- 通信途絶停止テスト
- 電圧低下停止テスト
- 子どもと遊ぶ前のチェックリスト

---

### 12.8 `docs/control_design.md`

記載内容:

- 倒立制御の基本式
- PD/PID構成
- 目標角度生成
- 速度制御
- 旋回制御
- エンコーダ速度推定
- 姿勢推定方式
- PIDチューニング手順
- ログに残すべき項目

---

### 12.9 `docs/communication_protocol.md`

記載内容:

- USBシリアルプロトコル
- JSON Lines形式
- UDP指令形式
- テレメトリ形式
- コマンド一覧
- エラー応答
- 通信途絶時の動作
- 複数台識別ID

---

### 12.10 `docs/tuning_log.md`

PID調整・構成変更の履歴を残すテンプレート。

```markdown
## YYYY-MM-DD Test N

### Hardware
- Motor:
- Wheel:
- Battery:
- IMU:
- Body height:
- Battery position:
- Weight:

### Parameters
- Kp:
- Ki:
- Kd:
- PWM limit:
- Target angle limit:

### Result
- Stable time:
- Oscillation:
- Drift:
- Fall direction:
- Notes:

### Next action
-
```

---

## 13. ファームウェア実装計画

### 13.1 開発環境

- PlatformIO
- Arduino framework for ESP32-S3
- Board: M5StampS3A / m5stack-stamps3相当
- C++実装

---

### 13.2 初期実装の優先順位

#### Milestone FW-0: プロジェクト雛形

- PlatformIOプロジェクト作成
- `main.cpp` 作成
- シリアルログ出力
- ビルド確認

#### Milestone FW-1: IMU読み取り

- BNO086読み取り
- ICM-42688-P読み取り
- BNO055読み取りは比較用として任意
- 角度・ジャイロ・加速度をJSON Linesで出力
- 静止時ノイズ確認

#### Milestone FW-2: モータードライバ

- 左右PWM出力
- DIR制御
- PWM上限設定
- モーター有効/無効
- 緊急停止
- 車体を浮かせた状態で回転確認

#### Milestone FW-3: エンコーダ

- A/B相読み取り
- 割り込み処理
- カウント方向確認
- 速度推定
- 左右差の確認

#### Milestone FW-4: バッテリー監視

- ADC分圧入力
- 電圧換算
- 低電圧警告
- 低電圧停止

#### Milestone FW-5: 安全管理

- 起動時モーター無効
- 傾き過大停止
- IMUタイムアウト停止
- 通信タイムアウト停止
- 手動停止
- 転倒後手動復帰

#### Milestone FW-6: 倒立PD制御

- BNO086姿勢角でPD制御
- Kiなし
- PWM上限付き
- 手支えテスト
- 短時間自立

#### Milestone FW-7: 速度制御

- エンコーダ速度推定
- 目標速度→目標角度変換
- 前進/後退
- 旋回
- 子どもモード

#### Milestone FW-8: UDP操作

- Wi-Fi接続
- UDP受信
- 指令パース
- テレメトリ返送
- 通信途絶停止

---

## 14. PCツール実装計画

### 14.1 `serial_monitor.py`

機能:

- USBシリアル接続
- JSON Lines読み取り
- ログ保存
- angle/gyro/pwm/encoder/batteryの簡易表示
- エラー行の表示

---

### 14.2 `pid_tuner.py`

機能:

- シリアルまたはUDP接続
- Kp/Ki/Kd変更
- PWM上限変更
- target angle limit変更
- motor enable/disable
- emergency stop
- リアルタイムグラフ
- ログ保存

最初はTkinter, PySide, Streamlit, Dash, またはCLIでもよい。  
実装速度重視ならCLI + matplotlibログ解析でも可。

---

### 14.3 `ps5_bridge.py`

機能:

- PS5 DualSense入力取得
- デッドゾーン処理
- スティック値を `vx`, `wz` に変換
- R2をデッドマンとして扱う
- ○ボタンで緊急停止
- UDPでM5StampS3Aへ送信
- 通信周期20〜50Hz程度

---

### 14.4 `log_plotter.py`

機能:

- JSON Linesログ読み込み
- angle/gyro/pwm/encoder/batteryをプロット
- 転倒直前の挙動確認
- PID調整比較
- センサー比較

---

## 15. 実機テスト計画

### 15.1 Test 1: 電源単体

目的:

- 電源配線ミスを防ぐ。
- 5V DCDCが安定しているか確認。
- M5StampS3Aが起動するか確認。

手順:

1. モーターは接続しない。
2. LiPoを接続。
3. メインスイッチON。
4. 5V出力測定。
5. M5StampS3A起動確認。
6. IMU認識確認。
7. 電圧監視値確認。

---

### 15.2 Test 2: IMU単体

目的:

- 姿勢角が正しく取れるか確認。
- 車体に固定した状態でノイズや振動を見る。

手順:

1. 車体を水平に置く。
2. pitch/roll/yawを記録。
3. 前後に傾ける。
4. 符号を確認。
5. BNO086とICM-42688-Pの値を比較。
6. 静止時のドリフトを記録。

---

### 15.3 Test 3: モーター単体

目的:

- 左右モーター、ドライバ、PWM、DIRの確認。
- 回転方向とエンコーダ方向を合わせる。

手順:

1. 車体を浮かせる。
2. 左モーター低PWMで正転。
3. 左モーター逆転。
4. 右モーター同様。
5. エンコーダカウント方向確認。
6. emergency stopで停止確認。

---

### 15.4 Test 4: 制御方向確認

目的:

- 倒立制御の符号ミスを防ぐ。

手順:

1. 車体を手で持つ。
2. モーターを有効にする。
3. 車体を前に傾ける。
4. 車輪が倒れる方向を追いかける向きに回るか確認。
5. 逆なら符号を修正。
6. 後ろ傾きも確認。

---

### 15.5 Test 5: 手支え倒立

目的:

- PD制御の初期応答確認。

手順:

1. Ki=0。
2. Kpを小さく開始。
3. Kdを小さく開始。
4. PWM上限を低く設定。
5. 手で支えて倒立位置へ。
6. 振動/遅れ/発散を確認。
7. ログ保存。

---

### 15.6 Test 6: 短時間自立

目標:

- 1秒自立
- 3秒自立
- 5秒自立
- 10秒自立
- 30秒自立

各回ログを保存し、PIDパラメータと車体条件を記録する。

---

### 15.7 Test 7: 低速走行

目標:

- 前進
- 後退
- その場旋回
- 低速カーブ
- 緊急停止
- 通信途絶停止

---

## 16. 開発時の重要な注意点

### 16.1 起動時にモーターを絶対に暴れさせない

対策:

- モーター有効信号は起動時安全側にする。
- GPIO初期状態を確認する。
- `setup()` の初期化完了までPWM出力しない。
- IMUキャリブレーション完了までモーター無効。
- ユーザー操作なしで自動的に倒立制御を始めない。

---

### 16.2 IMUの取り付け剛性

IMUが揺れると制御が破綻する。  
3Dプリントの薄い板や柔らかい外装にIMUを固定しない。

推奨:

- 車体中央の剛性部材に固定
- ネジ固定
- ケーブルの引っ張りで傾かない
- モーターやバッテリーから少し離す
- モーター配線とI2C/SPI配線を離す

---

### 16.3 バッテリーと重心

バッテリーは重く、重心に大きく影響する。  
上下に調整できる設計にする。

一般的傾向:

- 重心が高いと倒れる速度が遅くなり、初期制御が楽になる場合がある。
- 高すぎると転倒時衝撃が増える。
- 低すぎると素早い制御が必要になる。
- 実験シャーシではバッテリー位置調整が重要。

---

### 16.4 強いモーターはソフトウェアで制限する

25D HPモーターでも十分強い。  
初期は必ず以下を制限する。

- PWM最大値
- 目標角度最大値
- 速度指令最大値
- 旋回指令最大値
- 有効化条件
- 転倒後復帰条件

---

## 17. 親子で楽しむための要素

技術開発の本筋とは別に、モチベーション維持と遊びのために以下を入れる。

- ロボットに名前をつける。
- 起動時LEDアニメーション。
- 倒れたら赤く光る。
- 立てたら緑に光る。
- 低電圧で黄色く光る。
- ブザーで起動音・転倒音・勝利音。
- 交換式3Dプリント外装。
- 顔パーツ。
- ロケットブースター風外装。
- 紙ゲート、積み木、コーンでコース作成。
- スピードレベル1/2/3。
- こどもモード。
- レースモード。
- 複数台の色分けLED。
- カメラマウント。
- 将来的にFPVモニターで運転。

---

## 18. 直近のToDo

Codexに次に依頼するべき具体タスク。

### Task 1: ドキュメント生成

以下を作成する。

```text
docs/requirements.md
docs/architecture.md
docs/bom.md
docs/wiring.md
docs/gpio_assignment.md
docs/power_design.md
docs/safety_test_plan.md
docs/control_design.md
docs/communication_protocol.md
docs/tuning_log.md
```

この企画書の内容を分割・詳細化する。

---

### Task 2: 最終BOM表の作成

`docs/bom.md` に以下を整理する。

- 部品名
- 型番
- 数量
- 購入先候補
- 用途
- 採用理由
- 代替候補
- 未確定点
- 寸法図URL/データシートURL欄

---

### Task 3: GPIO割当案の作成

M5StampS3Aのピン仕様を確認し、以下を満たす割当を作る。

- 起動時安全
- PWM可能
- 割り込み可能
- ADC可能
- I2C/SPI共存
- 将来拡張余地あり

---

### Task 4: 電源配線図の作成

`docs/power_design.md` と `docs/wiring.md` に以下を作る。

- 2S LiPoからの電源分岐
- ヒューズ位置
- スイッチ位置
- MD10Cへの配線
- 5V DCDCへの配線
- M5StampS3Aへの5V供給
- ADC分圧
- GND方針
- コンデンサ配置

---

### Task 5: PlatformIO雛形

`firmware/` にプロジェクトを作る。

最初に実装する機能:

- Serial JSON Lines出力
- LED blink
- 設定定数
- 安全状態 `MOTOR_DISABLED`
- コマンドパーサの雛形
- モータードライバ抽象クラス
- IMU抽象クラス
- エンコーダ抽象クラス

---

### Task 6: PCツール雛形

`pc_tools/` に以下を作る。

- `requirements.txt`
- `serial_monitor.py`
- `pid_tuner.py` のCLI雛形
- `log_plotter.py`
- 後で `ps5_bridge.py`

---

## 19. Codexへの初回指示プロンプト案

Codexアプリに最初に渡す指示として、以下を使う。

```markdown
このプロジェクトは、M5StampS3Aを使った2輪セルフバランシングRCロボットの開発です。ルートにある企画書Markdownを読み、内容を落とさずにプロジェクト構造、設計ドキュメント、実装計画を整備してください。

まずは実装に入らず、以下を行ってください。

1. 企画書を読み、プロジェクトの目的・制約・採用部品・安全要件・将来拡張を把握する。
2. `docs/` 配下に以下のドキュメントを作成する。
   - `requirements.md`
   - `architecture.md`
   - `bom.md`
   - `wiring.md`
   - `gpio_assignment.md`
   - `power_design.md`
   - `safety_test_plan.md`
   - `control_design.md`
   - `communication_protocol.md`
   - `tuning_log.md`
3. まだファームウェア実装には入らず、各ドキュメントの整合性を確認する。
4. 未確定点、購入前に確認すべき点、実機で測るべき点を `docs/open_questions.md` に整理する。
5. 次のステップとして、PlatformIO雛形を作るための実装計画を `docs/implementation_plan.md` にまとめる。

注意:
- 安全性を最優先する。
- 起動直後にモーターが回らない設計を徹底する。
- PS5コントローラーはM5StampS3Aに直接接続せず、PC/Raspberry Pi中継方式とする。
- 初号機はUSBシリアルでログ取得・PID調整する。
- 車体内蔵充電は行わず、2S LiPoを外部バランス充電器で充電する。
- 最初の制御はPD制御から始め、Kiは後回しにする。
```

---

## 20. 成功基準

### Phase 1 成功基準

- M5StampS3Aが起動する。
- IMU値が安定して読める。
- 左右モーターを個別に低PWMで回せる。
- 左右エンコーダが読める。
- バッテリー電圧が読める。
- JSON Linesでログが出る。
- 緊急停止が効く。

---

### Phase 2 成功基準

- 手支え状態で制御方向が正しい。
- 短時間自立できる。
- 10秒以上自立できる。
- 倒れたらモーター停止する。
- PWM上限が効いている。
- PIDパラメータ変更ができる。

---

### Phase 3 成功基準

- 前進/後退ができる。
- その場旋回ができる。
- 低速こどもモードで安全に動く。
- 通信途絶で停止する。
- 低電圧で停止する。

---

### Phase 4 成功基準

- PCまたはRaspberry Pi経由でPS5コントローラー操作できる。
- デッドマン操作が機能する。
- 緊急停止が機能する。
- 複数台化に向けた車体ID設計ができている。

---

### Phase 5 成功基準

- 外装を交換できる。
- 子どもと安全に遊べる。
- 簡易コースを走れる。
- 将来的なカメラ搭載スペースがある。
- 複数台製作に向けたBOMとCADが整理されている。

---

## 21. 非目標

初号機では以下を目標にしない。

- 完全自律走行
- 自動起き上がり機構
- 屋外高速走行
- 車体内蔵2S LiPo充電
- M5StampS3AへのPS5コントローラー直接接続
- 高度な画像認識
- 最初から美しい外装
- 最初から複数台レース対応
- 最初からスマホアプリ完成

---

## 22. まとめ

最終的な推奨構成は以下。

```text
M5StampS3A
+ BNO086
+ ICM-42688-P
+ Pololu 25D 34:1 HP 12V Encoder付き ×2
  または Pololu 25D 47:1 HP 12V Encoder付き ×2
+ Cytron MD10C級 DCモータードライバ ×2
+ 2S LiPo 1500〜2200mAh XT30
+ 5V 3〜5A Buck DCDC
+ 80mm前後ゴムホイール
+ 3Dプリント調整式シャーシ
+ 外部RC用バランス充電器
+ LiPoセーフバッグ
+ ヒューズ・大電流スイッチ・電圧監視
```

開発順序は以下。

```text
1. ドキュメント整備
2. BOM確定
3. GPIO割当
4. 電源・配線図
5. CAD初号機ラフ設計
6. PlatformIO雛形
7. IMU単体テスト
8. モーター・エンコーダ単体テスト
9. 安全停止実装
10. 手支え倒立
11. 短時間自立
12. 速度制御
13. UDP操作
14. PS5コントローラー中継
15. 外装・複数台・カメラ拡張
```

このプロジェクトでは、安全性を最優先しつつ、M5StampS3Aを中心に、制御・電源・通信・機構・UI・ログ解析を段階的に統合していく。
